## TOR Career Uniques v1.7.39

- Stops Bannerlord's legacy "clan was destroyed" notifications from replaying on every save load.
- Persists TORCU's shared-clan migration completion inside the campaign save.
- Recognizes already-empty v1.7.38 legacy clans without invoking the native destruction action again.
- Still performs the full migration once for genuinely unmigrated saves.
- Retains the v1.7.38 shared-clan load-time improvement and all companion-set and Grey Lord/Cowl fixes.

Install by completely deleting the old `Modules/TORCareerUniques` folder, then extract the clean archive into the Bannerlord root directory.
